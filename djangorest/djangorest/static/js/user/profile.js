function init() {
    
    $('.dropdown-login, .dropdown-register').hide();
    $('.dropdown-logout, .all-blogs, .new-blog').show().trigger('show');
    
    $('.username').text(USERNAME);

}
