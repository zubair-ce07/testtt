# Library App Front End - React

## Set Up

Install the dependencies and devDependencies and start the server.

##### Add the API url in `src/settings.js` and then

```sh
$ yarn install
$ yarn start
```
