image: node:10.16
clone:
  depth: full
pipelines:
  branches:
    master:
      - step:
          script:
            - git remote set-url origin https://heroku:$HEROKU_API_KEY_FARAZ@git.heroku.com/$HEROKU_WITHCHATRTS_WEB.git
            - git push -f origin master